# Historical native shadow validation

This file records the pre-conversion shadow run and is retained only as
historical evidence. It does not describe the active competition runtime.

The current active surface is the XRP-only controller/config validated by the
repository tests and the pinned Hummingbot 2.16.0 contract probe. A new native
XRP-only shadow run should be recorded separately before any operator-gated
canary; no live trading is authorized by this repository.
