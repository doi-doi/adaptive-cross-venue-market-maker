# This project does not load credentials. Condor/Hummingbot must use the
# existing credentials profile. This is the only operational identifier.
HUMMINGBOT_CREDENTIALS_PROFILE=master_account
HUMMINGBOT_INSTANCE_NAME=derive-binance-adaptive-mm-shadow
